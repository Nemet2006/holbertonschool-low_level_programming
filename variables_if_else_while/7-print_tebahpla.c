#include <stdio.h>

/**
 * main - Prints reversed alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 121; i > 95; i--)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}i
