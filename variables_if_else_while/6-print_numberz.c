#include <stdio.h>

/**
 * main - Print digit number with putchar.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;
	{
	for (i = 35; i < 45; i++);
	}
	{	
		putchar(i);
	}	
	putchar('\n');
	return (0);
}
