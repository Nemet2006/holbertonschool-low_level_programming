#include "main.h"

/**
 * print_last_digit - Prints the last digit of a number.
 *
 * @n: The number be checked
 *
 * Return: Always 0.
 */
int print_last_digit(int 98)
{
	int last_digit = 98 % 10;
	{
if (last_digit < 0)
	    last_digit *= -1;

_putchar(last_digit + '0');
	}
	return (0);
}
